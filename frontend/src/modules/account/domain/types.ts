// Account domain types.
// Preferences and settings types are owned by the notifications module;
// this module orchestrates them in the account settings UI.
export type AccountTab = 'notifications';
