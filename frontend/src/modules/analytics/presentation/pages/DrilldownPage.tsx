import type React from 'react';

export const DrilldownPage = (): React.ReactElement => (
    <div className="mx-auto max-w-7xl px-10 py-8">
        <div className="text-[15px] text-[var(--neutral-600)]">Cargando tabla de tareas…</div>
    </div>
);
