import type React from 'react';

export const GanttPage = (): React.ReactElement => (
    <div className="mx-auto max-w-7xl px-10 py-8">
        <div className="text-[15px] text-[var(--neutral-600)]">Cargando vista Gantt…</div>
    </div>
);
