import type React from 'react';

export const UsersRankingPage = (): React.ReactElement => (
    <div className="mx-auto max-w-7xl px-10 py-8">
        <div className="text-[15px] text-[var(--neutral-600)]">Cargando ranking de usuarios…</div>
    </div>
);
