import type React from 'react';

export const ReportsPage = (): React.ReactElement => (
    <div className="mx-auto max-w-7xl px-10 py-8">
        <div className="text-[15px] text-[var(--neutral-600)]">Cargando generador de reportes…</div>
    </div>
);
